/*
#pragma once
#include "../nclgl/SceneNode.h"

class Rain : public SceneNode {
public:
	struct ParticleEffects {
		Vector3 fallSpeed;
		Vector3 pos;
		bool isRaining;

	};
	Rain(float x, float y, float num);
	void Update(float dt) override;
	GLuint tex;

protected:
	float type;
	float b;
	float numRain;
	vector<ParticleEffects> particles;


};
*/

